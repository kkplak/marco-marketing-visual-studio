// PostCSS configuration for Tailwind CSS. This CommonJS format is used because
// the Next.js build process expects a CommonJS module for PostCSS.
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {}
  }
};